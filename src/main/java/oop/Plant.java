package oop;

// When no access modifier is specified, the default access level for Plant class is package-private,
// meaning the class/method is accessible only within its own package.
class Plant {
    void print() {
        System.out.println("I'm a plant.");
    }
}
